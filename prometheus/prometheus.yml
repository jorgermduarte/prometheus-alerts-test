global:
  scrape_interval: 15s

rule_files:
  - "alerts.rules"

alerting:
  alertmanagers:
    - static_configs:
        - targets: ['alertmanager:9093']

scrape_configs:
  - job_name: 'app-node'
    static_configs:
      - targets: ['app-node-test:80']